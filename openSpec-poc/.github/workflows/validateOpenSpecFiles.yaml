name: ValidateOpenSpecFiles

on:
  push:
    paths:
      - 'specs/**'

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Validate YAML
        run: |
          pip install yamllint
          yamllint specs/