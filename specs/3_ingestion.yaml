id: ingestion-stage

type: beam_transform

description: Read raw data from BQ Table

input:
  source: BigQuery

output:
  type: PCollection<RawRecord>

logic:
  - read data from BigQuery table as PCollection<RawRecord>
  - take avsc file as input for schema validation
  -

validations:
  - schema validation
  - null checks